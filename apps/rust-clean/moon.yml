# https://moonrepo.dev/docs/config/project
# yaml-language-server: $schema=https://moonrepo.dev/schemas/project.json
$schema: "https://moonrepo.dev/schemas/project.json"

# Phase C: this project is now a Rust crate. moon doesn't have a
# first-class rust toolchain entry, so the language is "unknown" and
# tasks shell out to cargo directly.
layer: application
language: unknown
tags: ["app", "rust"]

id: "rust-clean"

project:
  title: "rust-clean"
  description: "_CHANGE_ME_DESCRIPTION_"

tasks:
  build:
    command: "cargo build -p rust-clean"
    options:
      runFromWorkspaceRoot: true

  build-release:
    command: "cargo build -p rust-clean --release"
    options:
      runFromWorkspaceRoot: true

  dev:
    command: "cargo run -p rust-clean"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  start:
    command: "cargo run -p rust-clean --release"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  test:
    command: "cargo test -p rust-clean"
    options:
      runFromWorkspaceRoot: true

  lint:
    command: "cargo clippy -p rust-clean --all-targets -- -D warnings"
    options:
      runFromWorkspaceRoot: true

  fmt-check:
    command: "cargo fmt -p rust-clean --check"
    options:
      runFromWorkspaceRoot: true

  # Apply all pending database migrations via sqlx-cli.
  # Requires `cargo install sqlx-cli --no-default-features --features postgres`.
  migrate:
    command: "sqlx migrate run --source apps/rust-clean/migrations"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  migrate-create:
    command: "sqlx migrate add --source apps/rust-clean/migrations"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  migrate-down:
    command: "sqlx migrate revert --source apps/rust-clean/migrations"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  migrate-reset:
    command: "sqlx database reset --source apps/rust-clean/migrations -y"
    options:
      runFromWorkspaceRoot: true
      envFile: "apps/rust-clean/.env"

  check-in-dance:
    command: 'echo "All is good, ready to go!"'
    deps: ["build", "migrate"]
    options:
      shell: true

  docker-build:
    script: |
      DOCKER_IMAGE="$(jq -r .name <$workspaceRoot'/package.json')-$project" \
      DOCKER_IMAGE_VERSION="$(jq -r .version <$workspaceRoot'/package.json')" \
      && docker build -f $projectRoot/Dockerfile . -t $DOCKER_IMAGE:$DOCKER_IMAGE_VERSION \
      && docker image list --filter reference=$DOCKER_IMAGE\*
    options:
      mergeEnv: replace
      runFromWorkspaceRoot: true
      interactive: false
      outputStyle: stream
      runInCI: false
      cache: false
      shell: true

  docker-run:
    script: |
      DOCKER_IMAGE="$(jq -r .name <$workspaceRoot'/package.json')-$project" \
      DOCKER_IMAGE_VERSION="$(jq -r .version <$workspaceRoot'/package.json')" \
      && docker run --network=host --rm -it --env-file $projectRoot/.env \
        --name $DOCKER_IMAGE $DOCKER_IMAGE:$DOCKER_IMAGE_VERSION
    options:
      mergeEnv: replace
      outputStyle: stream
      runFromWorkspaceRoot: true
      interactive: true
      runInCI: false
      cache: false
      shell: true

  docker-shell:
    script: |
      DOCKER_IMAGE="$(jq -r .name <$workspaceRoot'/package.json')-$project" \
      DOCKER_IMAGE_VERSION="$(jq -r .version <$workspaceRoot'/package.json')" \
      && docker run --network=host --rm -it --env-file $projectRoot/.env \
        --entrypoint sh $DOCKER_IMAGE:$DOCKER_IMAGE_VERSION
    options:
      mergeEnv: replace
      runFromWorkspaceRoot: true
      outputStyle: stream
      interactive: true
      runInCI: false
      cache: false
      shell: true
