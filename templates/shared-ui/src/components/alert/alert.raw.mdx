import { Canvas, Meta, Primary } from '@storybook/addon-docs/blocks';
import * as Stories from './alert.stories';

<Meta of={Stories} />

# Alert

Displays a callout for user attention with different severity levels.

## Example

```tsx
import { Alert, AlertTitle, AlertDescription } from '@repo/{{ package_name | kebab_case }}/components'
import * as Lucide from 'lucide-react'

// Basic usage
<Alert>
  <Lucide.Info className="size-4" />
  <AlertTitle>Heads up!</AlertTitle>
  <AlertDescription>
    This is an important message.
  </AlertDescription>
</Alert>

// Destructive variant
<Alert variant="destructive">
  <AlertCircle className="size-4" />
  <AlertTitle>Error</AlertTitle>
  <AlertDescription>
    Something went wrong!
  </AlertDescription>
</Alert>
```

## Preview

<Primary />
