# https://moonrepo.dev/docs/config/tasks
# yaml-language-server: $schema=https://moonrepo.dev/schemas/v2/tasks.json
$schema: 'https://moonrepo.dev/schemas/v2/tasks.json'

# Defines task deps that are implicitly inserted into all inherited tasks within a project.
# This is extremely useful for pre-building projects that are used extensively throughout
# the repo, or always building project dependencies.
# implicitDeps:
#   - '^:format'

taskOptions:
  runDepsInParallel: false

inheritedBy:
  language: tasks

tasks:
  env:
    command: printenv
    toolchain: system
    options:
      outputStyle: stream
      envFile: .env

  docker-images:
    script: |
      IMAGE_PREFIX="$(jq -r .name <$workspaceRoot'/package.json')" \
      && docker image list --filter reference=$IMAGE_PREFIX-$project\*
    options:
      mergeEnv: replace
      runFromWorkspaceRoot: true
      interactive: false
      outputStyle: stream
      runInCI: false
      cache: false
      shell: true
